import { ReactComponent as ProfileIcon } from 'assets/icons/profile.svg';

import Icon from './Icon';

const Profile = (props) => <Icon {...props} component={ProfileIcon} />;

export default Profile;
